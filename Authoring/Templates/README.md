# Authoring Templates

These templates back `./Scripts/scaffold_authoring.rb` and serve as copyable starter content for scenario authors.

They are intentionally simple:

- every generated file is valid YAML
- every generated file carries a schema modeline where supported
- the starter map validates without a soft-lock warning
- split event/interactable templates are safe to create incrementally
